/// <reference path="parcel-bundler.d.ts" />

import { join } from 'path';
// import Bundler from 'parcel-bundler';

test.skip('should bundle', async () => {
  // const bundler = new Bundler(join(__dirname, 'build', 'index.html'), {});
});
